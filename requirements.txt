# ── Core web app (chat health assistant) ────────────────────────────
fastapi
uvicorn[standard]
sqlalchemy
pydantic
python-dotenv

# ── AI (shared by chat + voice, modern unified SDK) ─────────────────
google-genai>=1.0

# ── Voice health assistant (real-time speech-to-speech) ─────────────
# Gemini Live handles VAD + turn-taking server-side, so we skip the heavy
# local `silero` / `local-smart-turn-v3` extras.
# Pin numba/llvmlite to versions with prebuilt wheels — newer numba resolves to
# a release-candidate llvmlite that has no wheel and fails to build from source.
numba==0.61.2
llvmlite==0.44.0
pipecat-ai[google,websocket,runner]>=0.0.108
langgraph>=1.0
numpy>=1.26
